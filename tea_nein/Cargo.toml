[package]
name = "tea_nein"
version = "0.1.0"
edition = "2021"

[dependencies]
itertools = "0.13.0"
counter = "0.6.0"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "all_predictors"
harness = false
