// https://projecteuler.net/problem=18
using System;

public class Hello {
    static public void Main() {
        int CurrentLineCount = 0;
        int CurrentLineLength = 1;
        for(var i = 0; i < 100; i++) {
            // I'm trying to print out a pyramid just like the one on the site but that just ascends so 
            // I can get a good data structure for this.
            Console.Write ("
        }
    }
}
