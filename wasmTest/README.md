How does WASM actually get built? Let's find out. 

Step 1 is to source the emscripten script thing so I can run the compiler. Do this with `source ~/Programming/emsdk/emsdk_env.sh`. 

OK now what?

Huh I stole some code from github and it just worked. Interesting. 
